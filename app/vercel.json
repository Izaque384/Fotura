{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)tests/**'",
  "crons": [
    {
      "path": "/api/cron/expirar",
      "schedule": "0 3 * * *"
    }
  ]
}
