name: One-time gallery UUID hardening
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: python3 .github/scripts/harden-gallery-uuid.py
      - run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/app/api/galeria/excluir/route.ts app/app/api/fotos/signed/route.ts app/app/api/galeria/publica/route.ts app/app/api/galeria/acesso/route.ts app/app/api/galeria/selecao/route.ts
          git commit -m "security: validate gallery uuid at api boundaries"
          git push origin main
